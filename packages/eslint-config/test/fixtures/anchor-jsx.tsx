/*
 * Separate from anchor.ts because TypeScript's "bundler" resolution treats
 * same-directory files that share a basename but differ only by extension
 * as ambiguous, silently dropping one from the program.
 */
export {};
