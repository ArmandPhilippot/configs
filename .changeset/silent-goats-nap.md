---
"@arphi/eslint-config": major
---

Updates the `typescript` preset to extend [`typescript-eslint`'s own `recommendedTypeChecked` preset](https://typescript-eslint.io/users/configs#recommended-type-checked).

You may see new lint results, since rules that weren't explicitly configured before are now inherited from that preset.
